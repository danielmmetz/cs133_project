<div class="center">
  <h1 class="center">Welcome <%= (Student.find current_user.student_id).name %></h1>
  <% room = student_room(current_user.student_id) %>
  <h3><%= if room.present? then "Your room is #{room.dorm_name}: ##{room.room_num}" else "You have not yet drawn a room" end %></h3>
</div>
